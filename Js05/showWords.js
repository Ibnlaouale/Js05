const mots = [ "Niger", "Niamey", "Sadore", "Codeloccol" ];

function showWords(){

        for(let i = 0; i<=mots.length; i++)
        console.log(mots[i]);
}

    showWords();

module.expots = showWords;